[project]
name = "bili-charge-monitor"
version = "0.1.0"
description = "Scanner service scaffold; fetching starts in M4."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"
