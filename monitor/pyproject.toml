[project]
name = "bili-charge-monitor"
version = "0.1.0"
description = "Bilibili charge dynamic scanner and per-UP process manager."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"
