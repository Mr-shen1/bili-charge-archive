# M5 本地验收记录

日期：2026-09-26。范围为内容查询接口、手机阅读页及登录回归；M6 媒体服务未实施。

| 项目 | 证据与结果 |
| --- | --- |
| UI-01～02 | `AuthHttpTest` 对未登录的动态、详情、媒体和管理路径返回 401；错误密码 401，正确密码建立会话，缺少 CSRF 的写请求 403。浏览器退出后访问详情显示登录页，错误密码提示失败，正确密码后详情恢复；根路径登录后进入动态列表，底部导航可进入 UP 管理并返回动态列表。通过 |
| UI-03 | 390×844 视口下单列列表和详情可读；切换“全部/M5 测试 UP”，详情内评论紧接动态；从详情返回恢复 `upUid`、cursor 和滚动位置。通过 |
| UI-04 | 开发库临时插入 21 条样例动态、21 条根评论、同一根下 21 条楼中楼；浏览器分别观察到 20+1、20+1、20+1，楼中楼分页不改变根评论页。通过 |
| UI-05 | 样例动态、根评论、楼中楼各自共享同一发布时间；浏览器首尾项分别为动态 21→1、根评论 21→1、楼中楼 1→21。真实 MySQL 测试核对前后页集合一致、无重复漏项。通过 |
| UI-06 | 样例首条动态在本站保存 21 根 + 21 楼中楼，卡片与详情均显示 42 条；根评论的来源回复数字段设为 999，展开入口使用本站实际保存的 21 条。通过 |
| UI-07（M5 范围） | 将样例动态及根评论标记来源不可用后，浏览器仍显示文字和“来源不可用”提示；PENDING 图片显示占位。按已确认边界，READY 图片实际显示归 M6 验证。本阶段通过 |
| UI-08 | 样例 UP 停用后，列表与详情仍可读并显示“UP 已停用”。通过 |
| 详情 URL | Nginx 下直接打开并刷新 `/dynamics/99000000000000000001`，仍显示详情与评论，无 404。通过 |

构建：`backend/mvnw.cmd -q test` 为 22 项通过，失败/错误 0；`frontend/npm run build` 通过。Spring 和 Nginx 开发镜像重建并启动，MySQL、Spring 和 Nginx 的 Compose 健康检查通过；测试后清理了样例 UP、群、21 条动态、42 条评论，原有真实采集数据未改动。

M5 未实现图片上传、OSS 签名访问或飞书投递。`UI-07` 的 READY 图片显示按已确认边界留待 M6 实际联调。
