# M0～M2 本地回归验收记录

- 日期：2026-09-26
- 范围：本机开发 Compose、隔离 MySQL 测试库、现有管理页面与管理 API。
- 结论：M0、M1 通过；M2 的配置功能通过本地阶段验收。`CFG-07` 中“删除路由后按默认群投递、停止固定扫描”的运行结果须等 M4、M7 实现后验证。
- 数据保护：未修改开发库中已有的飞书群、UP 和专属路由；未发送飞书消息。

## M0：工程与本地服务

| 检查 | 结果 |
| --- | --- |
| JDK 21 执行 `backend/mvnw.cmd -q test package` | 7 个测试套件、15 项测试通过，失败/错误/跳过均为 0；可打包 |
| `frontend/npm ci --registry=https://registry.npmjs.org`、`npm run build` | 依赖安装、TypeScript 检查、Vite 生产构建通过 |
| Python 3.12 执行 `unittest discover -s tests -v`、`pip wheel . --no-deps` | 1 项测试通过；wheel 构建成功 |
| `docker compose -f deploy/compose.dev.yaml config --quiet` 与 `ps` | 配置有效；`mysql8`、`spring-app`、`nginx` 均为 `healthy`，端口仅绑定 `127.0.0.1` |
| HTTP 首页与后端健康 | 首页 200；`/actuator/health` 返回 `UP` |

## M1：数据库与鉴权

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| `OPS-01` | 开发库有 10 张业务表、1 条成功的 Flyway V1 迁移；隔离 MySQL 测试验证重复迁移执行 0 项、空路由、相同双群及无效外键均受约束 | 通过 |
| `UI-01` | 匿名请求管理、动态及媒体 API 均为 401；经 Nginx 访问 `/internal/ups` 为 404，直连后端且不带令牌为 403 | 通过当前阶段的访问控制检查；内容与媒体接口将在 M5、M6 实现 |
| `UI-02` | 错误密码 401，正确登录 200，会话查询 200；Cookie 有 `Secure`、`HttpOnly`、`SameSite=Strict`；无 CSRF 的写请求 403，带令牌但参数无效的写请求 422 | 通过 |

## M2：配置管理

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| `CFG-01` | 隔离库测试用两个 UP 复用同一个群 ID，并为两个 UP 分别配置运维群；密文以 `v1:` 开头，公开群列表不含 Webhook | 通过 |
| `CFG-02` | HTTP 测试验证被引用群删除返回 409；手机群列表显示引用位置；无引用的测试群可删除 | 通过 |
| `CFG-03` | 对 UID `550494308` 的数字和空间 URL 实时预览均返回同一 UID；新增 UP 默认启用由测试验证 | 通过 |
| `CFG-04` | 服务、HTTP 和 MySQL 约束测试覆盖缺运维群、缺默认评论群、相同双群与无效外键 | 通过 |
| `CFG-05` | 真实预览动态 `1251460007328743432`，作者 UID 相符、类型为 `DYNAMIC_TYPE_DRAW` / `MAJOR_TYPE_DRAW`，评论目标为 `410108956` / `11`；固定响应测试拒绝他人、非充电、非目标类型及缺评论目标 | 通过 |
| `CFG-06` | HTTP 和服务测试拒绝空专属路由、相同双群；有效双群可保存 | 通过 |
| `CFG-07` | 测试验证专属路由可删除且列表不再包含该路由 | 配置操作通过；后续扫描与投递行为待 M4、M7 验证 |
| `CFG-08` | 390×844 浏览器中可见最近成功扫描、心跳、待发、失败及最近错误；无手动重扫/重发按钮 | 通过 |

手机宽度还检查了 UP 列表、UP 详情及飞书群页：布局可读，现有 UP 头像正常加载，群只显示“Webhook 已加密保存”。开发库现有 1 个群、1 个 UP、1 条专属路由；检查前后未改动。

## 凭据与后续边界

- `deploy/.env` 被 Git 忽略；本机配置的 Cookie、内部令牌、加密密钥及管理员密码哈希未在受检的已跟踪文本文件或 Spring/Nginx 容器日志中发现完全相同的值。开发库飞书群密文没有明文 URL 前缀，管理 API 不回显 Webhook。
- 原始需求 Markdown 与参考图片目录仍在；根目录 `AGENTS.md` 已按用户后续要求纳入 Git。验收清单原有“不创建 AGENTS.md”条件已不适用。
- Flyway 11.7.2 对 MySQL 8.4 报兼容性提示；本次迁移及约束测试通过，目标部署前仍应复核版本兼容。
- 本次不覆盖动态/评论查询、实际扫描、私有 OSS、飞书投递、2 GB 服务器及公网 HTTPS；它们分别属于后续里程碑。`UI-03～08`、`CRAWL-*`、`MSG-*` 和生产部署项未据此宣称通过。
