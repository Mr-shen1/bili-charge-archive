# M0 local development examples only. Replace values before any non-local deployment.
MYSQL_DATABASE=bili_charge_archive
MYSQL_USER=bili_dev
MYSQL_PASSWORD=replace-with-local-password
MYSQL_ROOT_PASSWORD=replace-with-local-root-password
APP_PORT=18080
WEB_PORT=18081

# M1 requires the admin and monitor values; M2 additionally requires the encryption key.
# For local Compose, place real values in the private deploy/.env file.
# Use a BCrypt hash, a monitor token of at least 32 characters, and a random 32-byte encryption key.
ADMIN_USERNAME=replace-with-admin-name
ADMIN_PASSWORD_BCRYPT=replace-with-bcrypt-hash
MONITOR_API_TOKEN=replace-with-at-least-32-random-characters
FEISHU_WEBHOOK_ENC_KEY=replace-with-base64-encoded-32-byte-key
# Optional until real Bilibili dynamic preview is needed.
BILI_COOKIE=replace-with-bili-cookie

# M6: configure all four values together for a private test bucket.
OSS_REGION=replace-with-region
OSS_BUCKET=replace-with-private-bucket
OSS_ACCESS_KEY_ID=replace-with-access-key-id
OSS_ACCESS_KEY_SECRET=replace-with-access-key-secret

# M7 will supply these only through a protected runtime configuration.
FEISHU_APP_ID=replace-with-app-id
FEISHU_APP_SECRET=replace-with-app-secret
