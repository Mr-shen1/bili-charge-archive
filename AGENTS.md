# 项目约定

## 入口与文档

- `README.md` 是项目入口；`类B站动态评论页.md` 和 `图片/` 是原始资料。`现有爬虫脚本/bili_charge_monitor.py`、`bili_charge_target_monitor.py` 是旧监控入口，只作解析、审计和消息格式的迁移参考；不要覆盖原始资料。
- 改需求时读 `docs/PRD.md`；改架构、表、接口或部署时读 `docs/ARCHITECTURE.md`、`DATABASE.md`、`API.md`、`DEPLOYMENT.md` 中相关文件；开发某个里程碑时读 `docs/IMPLEMENTATION_PLAN.md`，验收时读 `docs/ACCEPTANCE.md`。这些文档描述目标，不能视为功能已实现；需求冲突以 PRD 为准。

## 已确认的实现边界

- 后端基线为 Spring Boot 3.5.x、JDK 21、原生 MyBatis（兼容的 Starter 3.0.x）；具体补丁版本在建工程时固定。
- 仅采集指定 UP 的充电专属文字/图片动态及评论：每轮**先取最新 50 条再筛选**；有专属路由的动态另作固定目标。停用后重新启用只查当时最新 50 条和固定目标，不补抓其他历史动态，也不导入旧 JSON 状态或飞书队列。
- 每个 UP 的运维群必填，默认评论群至少配置一个；某条动态的专属路由也至少配置一个评论群。双群同时配置时必须不同；专属路由覆盖默认路由。若配置双群，动态概览和 UP 本人评论均发两群；新增路由不补发已处理内容。
- 每次抓取批次的内容、扫描进度与待发通知事件在同一 MySQL 事务中提交；B 站请求、OSS 上传和飞书发送在事务外。飞书按至少一次投递处理，极端故障可能重复，不能静默漏发。
- 网站内容只读，登录后从动态列表进入详情并直接查看评论；动态和评论图片进私有 OSS，图片失败先展示文字。旧脚本及路由文件含敏感凭据，不得把实际 Cookie、Webhook 或密钥复制到新代码、文档、测试或日志。

## Git 提交

- 提交说明采用 Conventional Commits：`type: 中文简述`，需要时写成 `type(scope): 中文简述`。常用类型为 `feat`、`fix`、`docs`、`test`、`refactor`、`chore`；一项提交只包含同一目的的改动。
